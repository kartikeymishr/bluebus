spring:
  datasource:
    username: root
    password: password
    url: jdbc:mysql://localhost:3306/bluebus?useSSL=false&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
    show-sql: 'false'
  security:
    user:
      password: testpassword

server:
  port: '8080'
